# Small test program: Verify that this does not compile.
IMPORT.ZUT zutHtmlAttributesCss.zut

FUNC Main() int
  # HTML attributes / Css
  ZUT.Css css = NEW().add("xxx")
  string fail = Testing.inAttributesCss(css).ToString()
  RETURN 0
}
