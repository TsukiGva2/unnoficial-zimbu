# Small test program: Verify that this does not compile.
IMPORT.ZUT zutUrlCss.zut

FUNC Main() int
  # URL / Css
  ZUT.Css css = NEW().add("xxx")
  string fail = Testing.inUrlCss(css).ToString()
  RETURN 0
}
