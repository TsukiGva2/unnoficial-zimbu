# Small test program: Verify that this does not compile.
IMPORT.ZUT zutUrlHtml.zut

FUNC Main() int
  # URL / Html
  ZUT.Html html = NEW().add("xxx")
  string fail = Testing.inUrlHtml(html).ToString()
  RETURN 0
}
