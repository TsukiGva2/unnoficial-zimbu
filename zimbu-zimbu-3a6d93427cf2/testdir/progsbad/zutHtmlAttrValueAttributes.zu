# Small test program: Verify that this does not compile.
IMPORT.ZUT zutHtmlAttrValueAttributes.zut

FUNC Main() int
  # HTML attribute value / Attributes
  ZUT.Attributes attr = NEW().add("xxx")
  string fail = Testing.inAttrValueAttributes(attr).ToString()
  RETURN 0
}
