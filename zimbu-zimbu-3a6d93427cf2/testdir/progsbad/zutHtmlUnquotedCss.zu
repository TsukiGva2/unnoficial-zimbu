# Small test program: Verify that this does not compile.
IMPORT.ZUT zutHtmlUnquotedCss.zut

FUNC Main() int
  # HTML attribute value / Css
  ZUT.Css css = NEW().add("xxx")
  string fail = Testing.inUnquotedCss(css).ToString()
  RETURN 0
}
